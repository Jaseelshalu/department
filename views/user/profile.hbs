<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        h1 {
            text-align: center;
        }
        .profile-container {
            display: flex;
            align-items: center;
            margin-top: 30px;
        }
        .avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 20px;
        }
        .user-details {
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <h1>User Profile</h1>
    <div class="profile-container">
        <img class="avatar" src="/images/user-images/{{userProfile._id}}.jpg" alt="User Avatar">
        <div class="user-details">
            <p><strong>Name:</strong> {{userProfile.Name}}</p>
            <p><strong>Email:</strong> {{userProfile.Email}}</p>
            <p><strong>Address:</strong> {{userProfile.Address}}</p>
            <p><strong>Age:</strong> {{userProfile.Age}}</p>
            <p><strong>Phone:</strong> {{userProfile.Phone}}</p>
            <p><strong>Place:</strong> {{userProfile.Place}}</p>
            <p><strong>District:</strong> {{userProfile.District}}</p>
            <p><strong>Date of Birth:</strong> {{userProfile.DOB}}</p>
            <p><strong>Institution:</strong> {{userProfile.Institution}}</p>
            <a href="/logout" class="btn logout btn-lg">Logout</a>
            <!-- Add more user details as needed -->
        </div>
    </div>
</body>
</html>